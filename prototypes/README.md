# Prototypes

Historical checkpoints from earlier explorations, retained for reference only.
None of this is part of the Signal production build or installation workflow
(see repository root README).

The AAC app starter previously archived here has moved to `/aac-app` at the
repo root — it's an active project now, not a historical checkpoint.
