# Prototypes

Historical checkpoints from earlier explorations, retained for reference only.
None of this is part of the Signal production build or installation workflow
(see repository root README).

- `aac-app-starter/` — an unrelated Expo/React Native prototype for an AAC
  (Augmentative and Alternative Communication) tap-to-speak app. Archived
  as-is from an uploaded ZIP; not connected to the Signal product.
