# Capture files

A capture is real research work, recorded, so the pipeline can be replayed by a
process that has no network egress. It is not a fixture in the sense of made-up
data: the searches here were really run and the results are really what came
back.

Each file states which parts are real and which are not, at the top of the file.
Read that before drawing a conclusion from a replayed run.
